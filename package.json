{
  "name": "stock-analyzer-worker",
  "version": "1.0.0",
  "description": "Yahoo Finance Stock Analysis API on Cloudflare Workers - Optimized for Bun",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "lint": "biome check --write",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "hono": "^4.9.12"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "@cloudflare/workers-types": "^4.20251011.0",
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.42.2"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
